on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.1.0
        with:
          server: ftp.theemailauthority.com
          username: githugftp@avemariafunds.com
          password: MDI5ODEwNjc5NTgyMDk
          server-dir: /
          log-level: verbose
          state-name: .clean-slate.json
          dry-run: true
